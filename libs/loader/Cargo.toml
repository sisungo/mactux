[package]
name = "loader"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
object = { version = "0.39", default-features = false, features = ["elf", "unaligned", "read", "std"] }
rand = "0.10"
rtenv = { path = "../rtenv" }
structures = { path = "../structures" }
tempfile = "3"
